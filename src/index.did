service: () -> {
    fetchArticles: () -> (vec record {status:variant {Draft; Published}; content:text; author:record {id:principal; username:text; email:text}}) query;
    getKeys: () -> (vec record {id:principal; username:text; email:text}) query;
    initApp: (text) -> ();
    isAdmin: () -> (bool) query;
    registerUser: (principal, text, text) -> (bool);
    transferAdmin: (principal) -> ();
}
